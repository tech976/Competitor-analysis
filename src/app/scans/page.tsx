import ComingSoon from "@/components/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Ad Scans"
      blurb="The full history of every scan across all clients, with status, ads found, and gap score. Recent scans are shown on the Dashboard today."
    />
  );
}
