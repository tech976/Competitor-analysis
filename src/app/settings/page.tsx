import ComingSoon from "@/components/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Settings"
      blurb="Agency settings, team members, API keys, and billing. Per-client brand profiles live inside each client today."
    />
  );
}
